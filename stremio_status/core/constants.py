from __future__ import annotations

from pathlib import Path

# Root of the stremio_status package
PACKAGE_DIR = Path(__file__).parent.parent

# Static files directory
STATIC_DIR = PACKAGE_DIR / "static"
